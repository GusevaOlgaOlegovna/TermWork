--Представление (Countries) Страны (просмотр всех стран, в которые есть путёвки):

CREATE VIEW Countries AS
SELECT Country FROM Country;

--Представление (Cities) Города (просмотр всех городов, в которые есть путёвки):

CREATE VIEW Cities AS
SELECT City FROM City;
